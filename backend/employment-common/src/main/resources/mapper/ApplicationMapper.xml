<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.employment.common.repository.ApplicationMapper">

    <select id="findByStudentId" resultType="com.example.employment.common.entity.Application">
        SELECT * FROM applications WHERE student_id = #{studentId} ORDER BY created_at DESC
    </select>

    <select id="findByJobId" resultType="com.example.employment.common.entity.Application">
        SELECT * FROM applications WHERE job_id = #{jobId} ORDER BY created_at DESC
    </select>

    <select id="findByCompanyId" resultType="com.example.employment.common.entity.Application">
        SELECT a.* FROM applications a
        INNER JOIN jobs j ON a.job_id = j.id
        WHERE j.company_id = #{companyId}
        <if test="status != null and status != ''">
            AND a.status = #{status}
        </if>
        ORDER BY a.created_at DESC
        LIMIT #{offset}, #{size}
    </select>

</mapper>