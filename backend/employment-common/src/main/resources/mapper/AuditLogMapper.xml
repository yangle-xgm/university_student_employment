<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.employment.common.repository.AuditLogMapper">

    <select id="findByActor" resultType="com.example.employment.common.entity.AuditLog">
        SELECT * FROM audit_logs WHERE actor_user_id = #{actorUserId} ORDER BY created_at DESC
    </select>

    <select id="findByActionAndTime" resultType="com.example.employment.common.entity.AuditLog">
        SELECT * FROM audit_logs
        <where>
            <if test="action != null and action != ''">
                AND action = #{action}
            </if>
            <if test="from != null">
                AND created_at >= #{from}
            </if>
            <if test="to != null">
                AND created_at &lt;= #{to}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>